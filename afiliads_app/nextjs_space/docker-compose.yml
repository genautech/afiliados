services:
  db:
    image: postgres:16.2-alpine
    restart: always
    environment:
      POSTGRES_DB: afiliads
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - db_data:/var/lib/postgresql/data

volumes:
  db_data:
